{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    },
    "Split": { // Split provider.
      "LogLevel": {
        "Default": "Warning", // Overrides preceding LogLevel:Default setting.
        "Microsoft": "Warning" // Debug:Microsoft.Hosting category.
      }
    }
  },
  "AllowedHosts": "*"
}
